import 'package:flutter/material.dart';

class ClosingCredits extends StatelessWidget {
  const ClosingCredits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text("</Anirudh Jayakumar>"),
        Text("built with 💜 using flutter."),
      ],
    );
  }
}
